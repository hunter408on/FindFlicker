package com.cvs.find_flicker.photo_detail

import com.cvs.find_flicker.R

enum class PhotoDetailViewIds(val id: Int) {
    Author(R.id.author),
    Title(R.id.title),
    ImageView(R.id.image_view)

}